# The test fixtures hold deliberately fake credential shapes — long enough to
# trip FlowTrace's own redaction rules, short enough not to be real provider
# keys. They are the test for the redaction that protects real ones.
paths-ignore:
  - "Sources/FlowTraceTests/Fixtures/**"
